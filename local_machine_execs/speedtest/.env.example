# Speedtest Tracker Configuration
# Copy this file to .env and update with your values

# Application Key (generate a new one using: php artisan key:generate)
# This is a sensitive secret key - keep it secure!
APP_KEY=base64:YOUR_GENERATED_KEY_HERE

# Database Password (only needed if using MySQL/PostgreSQL instead of SQLite)
# Leave empty if using SQLite (default)
DB_PASSWORD=
