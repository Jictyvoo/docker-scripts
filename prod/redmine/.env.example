# Redmine Configuration
# Copy this file to .env and update with your values

# MySQL Root Password (use a strong password)
# This is a sensitive password - keep it secure!
MYSQL_ROOT_PASSWORD=your_strong_root_password_here

# MySQL User Password (use a strong password)
# This is a sensitive password - keep it secure!
MYSQL_PASSWORD=your_strong_user_password_here

# Redmine Secret Key Base (generate a random 32-character hex string)
# You can generate one using: openssl rand -hex 16
# This is a sensitive secret key - keep it secure!
REDMINE_SECRET_KEY_BASE=your_secret_key_base_here
